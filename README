Instructions (Mac OS X):

1) Install Xcode and enable command-line tools (Preferences -> Downloads), or download and install the separate command-line tools for Xcode package

2) sudo port install qemu

3) make
